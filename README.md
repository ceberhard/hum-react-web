# hum-react-web
React web application for consuming the Hum Web API.

## Setup
1. clone repo
2. navigate to repo directory (contains package.json)
3. type `npm install`
4. to run app, type `yarn start`
5. begin coding


